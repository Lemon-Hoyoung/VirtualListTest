import RealList from '../pages/realList';
import VirtualList from '../pages/virtualList';

const routes = [
    {
        path: "/realList",
        component: RealList
    },
    {
        path: "/virtualList",
        component: VirtualList
    }
];

export default routes;